let selectedMode = 'phone';
document.querySelectorAll('.choice').forEach(button => button.addEventListener('click', () => { selectedMode = button.dataset.mode; document.querySelectorAll('.choice').forEach(item => item.classList.toggle('selected', item === button)); }));
document.getElementById('create').addEventListener('click', async () => { try { const response = await fetch('/api/room'); if (!response.ok) throw new Error('Unable to create session'); const data = await response.json(); document.getElementById('share').value = `${data.shareUrl}?mode=${selectedMode}`; document.getElementById('view').value = data.viewUrl; document.getElementById('links').classList.remove('hidden'); } catch (error) { alert(error.message); } });
document.querySelectorAll('[data-copy]').forEach(button => button.addEventListener('click', async () => { const input = document.getElementById(button.dataset.copy); if (!input) return; try { await navigator.clipboard.writeText(input.value); button.textContent = 'Copied'; setTimeout(() => { button.textContent = 'Copy'; }, 1400); } catch { input.select(); document.execCommand('copy'); } }));
